opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
pyautogui>=0.9.54

# Tobii 4C direct SDK mode (best accuracy, usually Windows + Python <= 3.10):
#   1. Tobii Experience / runtime installieren und in Tobii-Software kalibrieren
#   2. pip install tobii-research
#   3. python main.py --gaze-source tobii --skip-calibration
# tobii-research>=1.10.1
#
# Tobii 4C cursor mode (fallback when SDK is unavailable):
#   1. Tobii cursor control aktivieren
#   2. pip install pyautogui
#   3. python main.py --gaze-source cursor --skip-calibration

# L2CS-Net (optional, wenn kein Tobii):
# pip install torch torchvision l2cs
# Modell: https://drive.google.com/file/d/1eUr4OALR9K9P1WVuKTqBnXMa65JEm0Gh/
# Ablegen unter: Healthcare Code/models/L2CSNet_gaze360.pkl
